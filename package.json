{
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/ws": "^8.5.12",
    "typescript": "^5.5.4"
  },
  "type": "module",
  "dependencies": {
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "scripts": {
    "make-client": "docker build -t dante-relay-client:latest -f dockerfiles/client.Dockerfile .",
    "make-server": "docker build -t dante-relay-server:latest -f dockerfiles/server.Dockerfile ."
  }
}
